spring:
  application:
    name: Goblin Market
  datasource:
    url: jdbc:mariadb://localhost/goblinmarket
    username: root
    password: 
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true